# Alembic configuration for the tt spike.
#
# ``sqlalchemy.url`` is the default the ``alembic`` CLI uses when authoring a
# revision; the application overrides it per database in ``tt.schema.upgrade``.

[alembic]
script_location = alembic
prepend_sys_path = src
path_separator = os
# Timestamped, slugged filenames, so ``versions`` reads in order at a glance.
file_template = %%(year)d%%(month).2d%%(day).2d_%%(hour).2d%%(minute).2d_%%(slug)s_%%(rev)s
truncate_slug_length = 40
sqlalchemy.url = sqlite:///tt.db

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
