# Python bytecode / caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# uv / virtualenv
.venv/

# Packaging
*.egg-info/
build/
dist/

# macOS
.DS_Store

# The tracker's own database (the default TT_DB target)
tt.db
*.db
*.db-wal
*.db-shm

# Local SQLite exports/snapshots (real ticket data) — never commit.
backups/

# Real database secrets — never commit.
.env.prod
